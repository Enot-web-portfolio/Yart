import React, {FC} from 'react';
import classes from './DetailedModal.module.scss';
import {typedMemo} from "../../../../../core/utils/typed-memo";

type Props = Readonly<{

}>

const DetailedModalComponent: FC<Props> = (props) => null

export const DetailedModal = typedMemo(DetailedModalComponent);
