import React, { FC, memo } from 'react';

import classes from './UserAboutPage.module.scss';

const UserAboutPageComponent: FC = () => null;

export const UserAboutPage = memo(UserAboutPageComponent);
