import React, { FC, memo } from 'react';

import classes from './UserSettingsPage.module.scss';

const UserSettingsPageComponent: FC = () => null;

export const UserSettingsPage = memo(UserSettingsPageComponent);
