/** Skills Dto. */
export type SkillDto = Readonly<{

  /** Id skill. */
  id: number;

  /** Name. */
  name: string;

  /** Background color tag skill. */
  background_color: string;

  /** Font color tag skill. */
  font_color: string;

  /** Checked tag skill. */
  checked: boolean;
}>
