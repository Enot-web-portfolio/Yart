/** Values required to login. */
export type SignUp = Readonly<{

  /** Email. */
  email: string;

  /** Password. */
  password: string;

  /** User name. */
  firstName: string;

  /** User last name. */
  lastName: string;
}>;
